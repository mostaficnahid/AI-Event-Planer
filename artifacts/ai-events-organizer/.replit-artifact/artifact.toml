kind = "web"
previewPath = "/"
title = "AI Events Organizer"
version = "1.0.0"
id = "artifacts/ai-events-organizer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24336

[services.development]
run = "pnpm --filter @workspace/ai-events-organizer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-events-organizer", "run", "build" ]
publicDir = "artifacts/ai-events-organizer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24336"
BASE_PATH = "/"
